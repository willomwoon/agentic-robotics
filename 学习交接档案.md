# 具身智能学习 — 交接档案

> 用途：从"简历/求职"会话拆出的独立学习线。新会话里的 AI 读此档案即可无缝接续。

## 1. 目标
- 从物理硕士（理论计算方向）转型为 **具身智能 / Agentic Robotics** 研发。
- 最终投递目标：均胜集团「Agentic Robotics 研究员」。

## 2. 学员硬件与环境
- 电脑：i7-11700 / 16G RAM / 无独立显卡（Intel 核显）。
- 系统：Windows 64 位。
- 编辑器：PyCharm（解释器名 "Python 3.11 (Agentic Robotics)"）。
- Miniconda：装在 `C:\ProgramData\miniconda3`（base 是 Python 3.14，**不要用它**）。
- ⚠️ **`robotics` 环境的真实路径（2026-09-20 核实）**：
  `C:\Users\Administrator\.conda\envs\robotics\python.exe`
  —— 不在 `miniconda3\envs\` 下，别找错地方。已在 `jdk.table.xml` 里登记为项目解释器。
- 该环境实测版本：Python 3.11.16 / torch 2.14.0+cpu / numpy 2.4.6 /
  matplotlib 3.11.2（matplotlib 是 2026-09-20 现装的，用清华源）。

### 环境可靠性坑（Windows + PowerShell）
- 用 PowerShell 跑 python 时，**输出经常不回显**（ConPTY 问题）。
  可靠做法：让脚本自己把结果写成 UTF-8 文件，再用 Read 工具读。
- 为此项目里有个通用运行器 `tools/run_log.py`：
  `python tools/run_log.py day04_linear_regression.py`
  → 输出同时打屏 + 存到 `outputs/<脚本名>.log`（UTF-8，不乱码）。

## 3. 已完成进度（Day 1–4 全部完成）
- [x] Miniconda + `robotics` 环境搭建
- [x] PyTorch CPU 版 + NumPy 安装
- [x] GitHub 仓库：`willomwoon/agentic-robotics`（Public）
- [x] 张量创建/运算（`+` 逐元素、`@` 矩阵乘）
- [x] tensor ↔ NumPy 互转（共享内存，非拷贝）
- [x] shape 与 `view(-1)` 拉平
- [x] autograd：`requires_grad` / `backward()` / `.grad`（已理解"测坡度 vs 走到谷底"的区别）
- [x] **Day 4：手写线性回归 y = wx + b + 梯度下降**
  - 完整训练循环（前向 → backward → no_grad 更新 → 清零梯度）已手写并跑通
  - 手推偏导 ∂L/∂w、∂L/∂b，与 autograd 结果一致（-231.121521）
  - 实测：loss 381 → 0.430，w 0→2.986，b 0→2.029
  - **关键发现（务必保留）**：三个"解"必须分清 ——
    ① 造数据用的物理真值 (3.0, 2.0)；
    ② 含噪声数据的最小二乘谷底 (2.9631, 2.1846)，loss*=0.424439（闭式解算出）；
    ③ 梯度下降 500 轮时停在 (2.9860, 2.0293)，**还没到底**（ΔL=+0.0059）。
    500 轮时 b=2.029 看着比谷底 2.185「更接近真值 2.0」，纯粹是没收敛完的巧合。
    → 判据只能是「loss 还在不在降」，不能是「参数离真值近不近」。
  - 用 `optim.SGD` 复现同一结果，印证"optimizer 内部就是那两行减法"
  - **已建立的核心认知**：
    ① loss 曲面 = 势能面，梯度 = 力，训练 = 走下山谷；
    ② 学习率不是 U 形而是"单调变快 + 断崖"：临界值 = 2/λ_max(Hessian)；
       本数据下 λ=[0.478, 71.917]，临界 lr = 0.0278，0.03 就炸到 3.7×10¹⁹；
    ③ 条件数 κ=151（峡谷狭长）→ b 比 w 收敛慢得多（b 要 ~2000 轮才到底），
       这是 Momentum/Adam 的存在理由；
    ④ 单个 lr 无法同时伺候曲率差 151 倍的两个方向 → w 会过冲（第 50 轮冲到 3.16）。

## 4. 路线图（四阶段）
- [ ] Phase 1：PyTorch 与工程基础（2 周）—— 进行中（Day 1–4 已过）
- [ ] Phase 2：LLM Agent 核心（ReAct + Function Calling，3 周）
- [ ] Phase 3：LLM 驱动机器人仿真（PyBullet，4 周）
- [ ] Phase 4：项目开源 + 简历包装（2 周）

## 5. 下一步（Day 5）
- **用 PyTorch 官方组件重写同一个模型**，证明和手写版结果一致：
  `nn.Linear` + `nn.MSELoss` + `optim.SGD`（+ DataLoader 可选）。
- 目标：从"我手写过优化器"过渡到"我知道每个 API 背后在干什么"，
  尤其是 `zero_grad()` 为什么必须调用、`nn.Module` 的 `parameters()` 是怎么回事。
- 之后 Day 6：Momentum / Adam 对比实验，直接解决 Day 4 暴露的 κ=151 峡谷问题。

## 5.1 项目文件约定（2026-09-20 起）
- 脚本命名用 `dayNN_主题.py`（如 `day04_linear_regression.py`），
  **不要**再用 `9.20.py` 这种日期命名——Phase 4 要开源，命名要能自解释。
- `tools/run_log.py`：通用运行器（跑脚本 + 存 UTF-8 日志）。
- `outputs/`：图 + 日志输出目录。
- `notes/dayNN_主题.md`：每日要点笔记，含手推公式和实测数据。
- 每个脚本末尾都有"课后实验"清单，学员必须自己改代码重跑并解释现象。

## 6. 教学偏好（重要）
- 每天可投入 **1–2 小时**。
- 要求 AI **逐概念讲解**，多用**物理直觉类比**（学员物理背景强）。
- 节奏：稳扎稳打，不催进度。
- 学员当前薄弱项：工程化经验、PyTorch 实战、Linux、LLM 项目（均为待补，非能力缺陷）。
- **教学纪律（2026-09-20 教训）**：宁可承认自己前面的说法错了，也不要为了叙事漂亮
  而教一个错的模型。本次 Day 4 曾一度把学习率行为写成"U 形"，
  实测后发现凸二次曲面下是"单调 + 断崖"，已当场纠正。
  **所有教学结论必须用实际跑出来的数据背书，不能凭印象写。**
